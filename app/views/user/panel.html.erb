<!DOCTYPE html>
<html>
    
    <head>
        <title>Program Finder</title>
    </head>
    
    <body>
        <header class="navbar navbar-fixed-top navbar-inverse">
            <div class="container">
                <nav>
                    <ul class="nav navbar-nav navbar-right">
                        <li><%= link_to "Support",    '#' %></li>
                    </ul>
                </nav>
            </div>
        </header>
        
        <div class ="container">
            <div class ="center jumbotron">
                <h2>Welcome, <% @user.first_name %></h2>
            </div>
        </div>
        
        <div class ="container">
            <div class="col-md-6 col-md-offset-3">
                <% if @user.administrator %>
                    <p><%= link_to "Add User", {:action => 'new'} %></p>
                <% end %>
            </div>
        </div>
				<div class="container">
					<div class="center jumbotron">
						<button><%= link_to "Add Program", {:controller => 'programs', :action => 'new'} %></button>
					</div>
					<div class="center jumbotron">
						<button><%= link_to "Add Program", {:controller => 'program_requirement', :action => 'new'} %></button>
					</div>
				</div>
    </body>
    
</html>